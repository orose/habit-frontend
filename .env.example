# Copy to .env.local to point a local `npm run dev`/`npm run build` at a
# standalone backend instead of the Vite dev proxy. Leave unset (or empty)
# to keep API calls relative (/v1/...) - the normal local dev setup.
VITE_API_BASE_URL=http://localhost:8080
